"# ===============================================
# TalentHub Frontend Environment Configuration
# ===============================================

# ----------------
# Backend API URL
# ----------------

# Production (with domain)
REACT_APP_BACKEND_URL=https://yourdomain.com/api

# Production (with IP address)
# REACT_APP_BACKEND_URL=http://YOUR_SERVER_IP/api

# Development (local backend)
# REACT_APP_BACKEND_URL=http://localhost:8001/api

# Staging
# REACT_APP_BACKEND_URL=https://staging.yourdomain.com/api

# ----------------
# IMPORTANT NOTES
# ----------------
# 1. The backend URL must end with /api (no trailing slash after /api)
# 2. Use HTTPS in production for security
# 3. Make sure CORS is configured properly in backend
# 4. Never commit this file with production values to public repositories
# 5. Copy this file to .env and fill in your actual values
# 6. Restart the frontend after changing environment variables

# ----------------
# Next.js Environment Variables Info
# ----------------
# - Variables prefixed with REACT_APP_ or NEXT_PUBLIC_ are exposed to the browser
# - All other variables are server-side only
# - Changes require rebuild: yarn build && yarn start
"
